function Search() {
  return (
    //this.state should have a variable that will be set to this value
    <input type="text" value={this.state.value} placeholder="search up an artist"></input>
  )
}

export default Search
